/**
 * No idea what to do here yet, so here's some filler.
 * TODO Make an actual sound model.
 */
export interface Sound {
  Tone: string[];
  type: string;
  duration: number;
  repeating: boolean;
}
